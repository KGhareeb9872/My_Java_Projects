//Kathleen Ghareeb: Assign 7, CalculatorWithStats.java 

// extends super and implements interface
class CalculatorWithStats extends Calculator implements StatCalculator  //no public 
{
	
	
		int sum = 0;
		

	public double avg(int[] x);
	{
		// -- > wrong x = newX;
		int sum = 0;
		
		 for (int i = 0; i < newX.length; i++)
		 	{
		 		sum += i;
		 		
		 		// sum = sum+xArray[i]
		 		
		 		double avergae = sum/x; // xArray.length 
		 		
		 		return average;
		 	
		 	}
		
	}


}
		 